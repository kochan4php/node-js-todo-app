<div class="empty-state is-hidden" id="empty-filtered">
    <span class="empty-art" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="11" cy="11" r="7" />
            <path d="M21 21l-4.3-4.3" />
        </svg>
    </span>
    <h2>Tidak ada hasil</h2>
    <p id="empty-filter-text">Coba kata kunci lain atau ubah filternya.</p>
    <button type="button" class="btn btn-ghost" id="empty-clear">Hapus pencarian &amp; filter</button>
</div>